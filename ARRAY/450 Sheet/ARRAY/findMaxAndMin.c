#include<stdio.h>
int checkMinMax(int arr[],int n,int max,int min){
    //1. for looop 
   /*  for(int i = 1; i < n; i++){
        if(max < arr[i]){
            max = arr[i];
        }
        if(min > arr[i]){
            min = arr[i];
        }
    }
    printf(" \n Max  = %d \n Min = %d ",max,min); */

    // 2. 
}
void main(){
     int arr[] = {12,34,54,64,34,78};
    int n = 6;
    int max = arr[0];
    int min = arr[0];
    // 1 . using for loop 
   // checkMinMax(arr,n,max,min);
}